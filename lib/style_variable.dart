import 'package:flutter/material.dart';

class StyleVariables extends StatelessWidget {
  const StyleVariables({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
